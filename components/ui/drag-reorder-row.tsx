import { type ReactNode, useMemo, useRef } from 'react';
import { Animated, PanResponder, Pressable, View } from 'react-native';
import { Palette, Radius, Space } from '@/constants/design';
export function DragReorderRow({ children, header, label, onMove, canMoveDown, canMoveUp }: { children: ReactNode; header: ReactNode; label: string; onMove: (offset: -1 | 1) => void; canMoveDown: boolean; canMoveUp: boolean }) {
  const translateY = useRef(new Animated.Value(0)).current; const moved = useRef(false);
  const responder = useMemo(() => PanResponder.create({ onStartShouldSetPanResponder: () => true, onPanResponderGrant: () => { moved.current = false; }, onPanResponderMove: (_, gesture) => { translateY.setValue(gesture.dy); if (!moved.current && gesture.dy < -28 && canMoveUp) { moved.current = true; onMove(-1); translateY.setValue(0); } if (!moved.current && gesture.dy > 28 && canMoveDown) { moved.current = true; onMove(1); translateY.setValue(0); } }, onPanResponderRelease: (_, gesture) => { if (!moved.current && gesture.dy < -28 && canMoveUp) onMove(-1); if (!moved.current && gesture.dy > 28 && canMoveDown) onMove(1); Animated.spring(translateY, { toValue: 0, useNativeDriver: true }).start(); }, onPanResponderTerminate: () => Animated.spring(translateY, { toValue: 0, useNativeDriver: true }).start() }), [canMoveDown, canMoveUp, onMove, translateY]);
  return <Animated.View style={{ backgroundColor: Palette.surface, borderColor: Palette.border, borderRadius: Radius.small, borderWidth: 1, gap: Space.xs, paddingHorizontal: Space.sm, paddingVertical: Space.xs, position: 'relative', transform: [{ translateY }], zIndex: 2 }}><View style={{ justifyContent: 'center', minHeight: 28, paddingRight: 44 }}>{header}</View><Pressable accessibilityActions={[...(canMoveUp ? [{ name: 'decrement' as const, label: `Move ${label} earlier` }] : []), ...(canMoveDown ? [{ name: 'increment' as const, label: `Move ${label} later` }] : [])]} accessibilityLabel={`Reorder ${label}`} accessibilityRole="adjustable" onAccessibilityAction={(event) => event.nativeEvent.actionName === 'decrement' ? onMove(-1) : event.nativeEvent.actionName === 'increment' ? onMove(1) : undefined} {...responder.panHandlers} style={({ pressed }) => ({ alignItems: 'center', height: 44, justifyContent: 'center', opacity: pressed ? 0.55 : 1, position: 'absolute', right: Space.xs, top: 0, width: 44 })}><View accessibilityElementsHidden importantForAccessibility="no-hide-descendants" style={{ gap: 3 }}>{[0, 1, 2].map((bar) => <View key={bar} style={{ backgroundColor: Palette.textMuted, borderRadius: 1, height: 2, width: 20 }} />)}</View></Pressable>{children}</Animated.View>;
}
